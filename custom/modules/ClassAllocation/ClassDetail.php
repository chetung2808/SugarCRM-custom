<?php


if (!defined('sugarEntry') || !sugarEntry) die('Not A Valid Entry Point');

class ClassDetail
{
    public function showDetail($classId)
    {
        // Xử lý logic hiển thị thông tin chi tiết về lớp học với ID $classId
        // Ví dụ: hiển thị tên lớp, giáo viên, số lượng học viên, v.v.
    }
}
